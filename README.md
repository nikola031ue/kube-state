# kube-state

Kubernetes cluster state management for ShopHub platform.

## Structure

- `clusters/local/` - Local development cluster (minikube)
  - `cluster.yaml` - Cluster metadata
  - `shop-operator/` - Shop Kubernetes operator
  - `shophub/` - ShopHub management platform
  - `shophub-discord/` - ShopHub Discord notifications
