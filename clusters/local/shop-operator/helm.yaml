apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: shop-operator
  namespace: flux-system
spec:
  chart:
    spec:
      chart: ./charts/shop-operator
      sourceRef:
        kind: GitRepository
        name: shophub-charts
  interval: 1h
