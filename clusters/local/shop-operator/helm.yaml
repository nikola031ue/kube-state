apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: shop-operator
  namespace: shop-operator-system
spec:
  chart:
    spec:
      chart: ./shop-operator
      sourceRef:
        kind: HelmRepository
        name: shophub-charts
      version: ">=0.1.0"
  interval: 1h
